name: hermes-memory-zh
version: 0.2.0
description: "Chinese semantic memory provider for Hermes Agent — jieba tokenization + bge-m3 embedding via OpenAI-compatible gateway. Fork of official holographic memory plugin."
hooks:
  - on_session_end
