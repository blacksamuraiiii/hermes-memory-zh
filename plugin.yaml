name: hermes-memory-zh
version: 0.1.0
description: "Chinese semantic memory provider for Hermes Agent — jieba tokenization + embedding via an OpenAI-compatible gateway. Fork of official holographic memory plugin."
hooks:
  - on_session_end
