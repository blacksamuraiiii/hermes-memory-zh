name: hermes-memory-zh
version: 0.2.0
description: "Chinese memory provider for Hermes Agent — lightweight, local SQLite, jieba Chinese tokenization; optional embedding via OpenAI-compatible gateway. Fork of official holographic memory plugin."
hooks:
  - on_session_end
